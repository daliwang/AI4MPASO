# Selected restart ocean-state variables for AI training
# Source header: restart/mpas_ocean_header.txt
# Sample restarts:
#   v3.GMPAS-NYF_QU240.mpaso.rst.0661-01-01_00000.nc  (early / input exemplar)
#   v3.GMPAS-NYF_QU240.mpaso.rst.0681-01-01_00000.nc  (later / target exemplar)
#
# Role: SAME variable set used as
#   INPUT  from ~50-year restart state
#   TARGET from ~600-year restart state
# Focus: deep ocean (2000 m to bottom), motivated by OHC anomaly
#   (Ocean_EQ.png: persistent cooling in 2000m–bottom band during spinup)

# --- Core 3D prognostic state (mask to levels with depth > 2000 m) ---
temperature
salinity
layerThickness
normalVelocity

# --- Column / vertical geometry (needed to select deep layers & integrate OHC) ---
bottomDepth
maxLevelCell
minLevelCell
refBottomDepth
restingThickness

# --- Deep vertical levels on this mesh (refBottomDepth > 2000 m) ---
# 1-based k = 46..60 (refBottomDepth from ~2075 m to ~5500 m)
# 0-based k = 45..59
